defmodule Api.CryptoCoins do
  # Check Tesla Docs
  # API Base is: "https://api.coinranking.com/v1/public"
  # Endpoint is: /coins

  # returns {:ok, array_of_coins}
  def coins() do
  end

  # returns array_of_coins
  #raise if error
  def coins!() do
  end
end
